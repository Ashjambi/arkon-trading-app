const fs = require('fs');
let code = fs.readFileSync('UPDATES_LOG.md', 'utf8');

const newEntry = `* **الإغلاق المُجمّع والتتبع الذكي للسلة (Basket Smart Trailing TP):**
  * تم تطوير وتفعيل **نظام السلة (Basket Management)** المتقدم.
  * في حال وجود صفقات متعددة من نفس النوع (مثلاً: شبكة تعزيز أو Pyramiding لصفقات الشراء)، يقوم النظام بجمع أرباح/خسائر السلة كاملة.
  * إذا وصل إجمالي ربح السلة للهدف (مع تخفيض طفيف لتسريع الإغلاق)، يبدأ نظام الـ Trailing (التتبع) على مستوى السلة ككل بدلاً من الصفقات الفردية.
  * **الفائدة الكبرى:** هذا يسمح للصفقات الرابحة بسحب وإنقاذ الصفقات الخاسرة (مثل أول صفقة في الشبكة) وإغلاقها جميعاً بمجرد تحقيق قمة ربحية للسلة، مما يُنظف الشارت بشكل أسرع ويقلل الـ Drawdown بشكل جذري.
`;

code = code.replace('* **التعزيز الهرمي الذكي', newEntry + '* **التعزيز الهرمي الذكي');
fs.writeFileSync('UPDATES_LOG.md', code);
console.log("Fixed log");
